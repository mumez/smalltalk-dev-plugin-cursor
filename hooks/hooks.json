{
  "version": 1,
  "hooks": {
    "postToolUse": [
      {
        "command": "uv run --no-project python ${CURSOR_PLUGIN_ROOT}/scripts/suggest-class-comment.py",
        "matcher": "Write",
        "timeout": 30
      }
    ]
  }
}
